import { concat } from 'adminv2/helpers/concat';
import { module, test } from 'qunit';

module('Unit | Helper | concat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concat([42]);
  assert.ok(result);
});
