import { icon } from 'adminv2/helpers/icon';
import { module, test } from 'qunit';

module('Unit | Helper | icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = icon([42]);
  assert.ok(result);
});
