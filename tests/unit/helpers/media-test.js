import { media } from 'adminv2/helpers/media';
import { module, test } from 'qunit';

module('Unit | Helper | media');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = media([42]);
  assert.ok(result);
});
